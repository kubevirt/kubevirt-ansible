---
apiVersion: v1
kind: Template
metadata:
  creationTimestamp: null
  name: cdi-test
objects:
- apiVersion: cdi.kubevirt.io/v1alpha1
  kind: DataVolume
  metadata:
    name: ${RESOURCE_NAME}
  spec:
    source:
      pvc:
        namespace: ${SOURCE_NS}
        name: ${SOURCE_NAME}
    pvc:
      #storageClassName: "hostpath"
      accessModes:
      - ReadWriteOnce
      resources:
        requests:
          storage: 500Mi
parameters:
- name: RESOURCE_NAME
- name: SOURCE_NS
- name: SOURCE_NAME

